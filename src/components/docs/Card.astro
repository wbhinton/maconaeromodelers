---
interface Props {
  title: string;
  icon?: string;
  href?: string;
}
const { title, href } = Astro.props;
---
<a href={href} class="block not-prose p-6 bg-white border border-gray-200 rounded-lg shadow-sm hover:shadow-md hover:border-primary transition-all no-underline group">
    <h3 class="text-lg font-bold text-gray-900 group-hover:text-primary mb-2 flex items-center gap-2">
      <span>📄</span> 
      {title}
    </h3>
    <p class="text-gray-600 text-sm">
        <slot />
    </p>
</a>